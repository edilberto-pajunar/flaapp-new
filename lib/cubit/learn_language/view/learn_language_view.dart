import 'package:flutter/material.dart';

class LearnLanguageView extends StatelessWidget {
  const LearnLanguageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

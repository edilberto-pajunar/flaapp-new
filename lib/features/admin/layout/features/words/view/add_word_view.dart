import 'package:flutter/material.dart';

class AdminAddWordView extends StatelessWidget {
  const AdminAddWordView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Word"),
      ),
      body: Column(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          
        ],
      ),
    );
  }
}
